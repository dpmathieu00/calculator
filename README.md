# Calculator

This is a Java-based application that uses the JavaFX framework to provide the capabilities of a standard calculator.

## Features
-Calculator automatically detects the order of operations native to any particular equation input by the user and produces results that are consistent with that rule set, unless otherwise specified by the indication of parentheses.

